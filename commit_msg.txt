chore(ci): commit all current changes to master after deep CI analysis (qmcoxide-nyv)

- Local full quality gates green: cargo check --lib; cargo test --all (15 tests ok); cargo clippy --all-targets -- -D warnings
- Advances remote master (PR#1 head) so new CI run uses current on-disk tree (passing state)
- Collation of roots from analysis: PR master->main with ci on [main] (triggers via base); all jobs fail at "Cargo test (lib + bins + tests)" step (exit 101 ubuntu/mac, 1 win) despite check --lib pass on CI + identical local pass; committed lock at 0ae6a805 head suspect (MCP get returned non-lock placeholder text); mac install sets CC=clang (not full llvm path) + incomplete LDFLAGS; win redirect to 2025-vs2026 image; possible src tree drift (some modules) between local disk and prior head commit; node20 deprecation notices (FORCE present but checkout warns); no fatal in annotations visible without login.
- This local commit + follow-up MCP push_files to master will put the exact passing disk state (good lock, full src, ci with installs/pin) on remote master, advancing PR head and re-triggering matrix CI.
- Follows AGENTS/CLAUDE mandatorily: bd ONLY for tracking (updated nyv), NEVER auto index, explicit cmds only, temp fixtures in tests, path fidelity, quality gates before push, mandatory git protocol + status "up to date", keep [Unreleased], cross-plat, no direct DB.

Refs bd qmcoxide-nyv, PR#1, previous run 26940147891, local rustc 1.88.0.

Per user "commit all changes to master branch" + "perform a deep analysis".
